import type { Meta } from '@storybook/react';

import { Post } from './Post';

const meta: Meta<typeof Post> = {
    component: Post
}

export default meta

export const PostContetn = (props: any) => {
    return <div></div>
}